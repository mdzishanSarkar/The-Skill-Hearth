services:
  mongo:
    image: mongo
    container_name: skillshare-mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db

  redis:
    image: redis
    container_name: skillshare-redis
    ports:
      - "6379:6379"

volumes:
  mongo-data:
